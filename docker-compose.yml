services:
  kpfc:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env.docker
    restart: unless-stopped
